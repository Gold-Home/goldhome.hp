[project]
name = "goldhome-image-gen"
version = "0.1.0"
description = "Gold Home HP用の画像自動生成スクリプト（Gemini API）"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.0.0",
    "python-dotenv>=1.0.0",
    "pillow>=10.0.0",
]
